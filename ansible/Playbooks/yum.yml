--- #Yum Module Example
- hosts: apacheweb
  user: ansible
  sudo: yes 
  connection: ssh
  gather_facts: no
  tasks:
  - name: Install the apacheweb Server
    action: yum name=* state=latest
  
  


